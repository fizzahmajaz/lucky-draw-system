package com.fizzah.lucky_draw_system.repository;

import org.springframework.stereotype.Repository;

@Repository
public interface DashboardRepository {
    // This is a placeholder - you can implement a custom repository class (DashboardRepositoryImpl)
    // with EntityManager queries. For now we'll use WinnerHistoryRepository::findTopWinnerNative
}
